#!/bin/zsh
. <%= @stdlib %>

std/root

function init_start() {
	rm -rf /mnt/batch
	mkdir -p /mnt/batch
        mount -t tmpfs batch /mnt/batch -o defaults,size=8G
}

function init_stop() {
        umount /mnt/batch
}

init_$1
