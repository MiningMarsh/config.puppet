check file <%= @title %> with path <%= @binary %>
      start program = "nohup <%= @binary %> <%= @arguments %> > /dev/null 2>&1"
      stop  program = "killall <%= @binary %>"
      if changed sha1 checksum then restart
